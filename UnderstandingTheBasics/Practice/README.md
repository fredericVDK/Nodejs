# Practice: Node.js Basics Assignment

Run the server from this folder:

```
node app.js
```

The server listens on port `3000`.

- `GET /` : greeting page with a username form
- `GET /users` : list of dummy users
- `POST /create-user` : accepts `username` and logs it to the console
